apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloud-inventory-management.fullname" . }}
  labels:
    {{- include "cloud-inventory-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cloud-inventory-management.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
