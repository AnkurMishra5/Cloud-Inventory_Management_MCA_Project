apiVersion: v1
kind: Service
metadata:
  name: {{ include "storlogix.fullname" . }}
  labels:
    {{- include "storlogix.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "storlogix.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
